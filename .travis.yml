language: node_js
node_js:
  - '10'

cache:
  directories:
    - node_modules

before_install:
  - npm cache verify
  - npm prune
  - npm update

install:
  - npm install

script:
  - npm run ci:travis
